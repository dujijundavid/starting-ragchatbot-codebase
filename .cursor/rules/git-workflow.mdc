---
description: Enforce semantic versioning and project-specific Git hygiene.
tags:
  - git
  - process
alwaysApply: false
---
## 🧠 Git Workflow

- **Message format:** 严格使用 Conventional Commits（`feat:`, `fix:`, `refactor:`, `chore:`, `docs:`, `test:` 等），subject 以动词原形开头，长度控制在 72 个字符以内。
- **Scope & batching:** 将相关改动归为单个 commit；如涉及多个独立问题，拆分成多个提交并保持每个提交可独立回滚。
- **Commit body:** 对于非微小变更补充正文，说明 `why`（原因）、`how`（主要调整）、`impact`（影响范围或迁移提示），必要时使用项目通用语言。
- **Diff awareness:** 先运行 `git status -sb` 与 `git diff --stat` 检查暂存范围，避免提交未完成或冗余文件；自动生成信息语义不足时手动补充说明。
- **Quality gates:** 在提交前确认 lint（如 `eslint`, `prettier`）与测试套件（`jest`, `vitest`, `pytest` 等）通过，或明确列出待办与风险。
- **Failure handling:** 若检查失败，优先修复或给出具体的后续建议，必要时暂缓提交并记录阻塞原因。
- **Amend & review:** 提交后若发现遗漏，使用 `git commit --amend` 或追加 fixup commit，并在推送前复查提交记录与说明。
